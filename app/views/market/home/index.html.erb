
<div class="row">
    <div class="col-md-2 col-md-offset-5">
      <%= link_to "选择网站", "#", class: "btn btn-default btn-block btn-lg"%>
      <% @market_sites.each do |site| %>
        <%= link_to site.name, "choose_site?id=#{site.id}", class: "btn btn-info btn-block btn-lg"%>
      <% end %>
      <%= link_to "添加网站", new_market_site_path, class: "btn btn-danger btn-block btn-lg"%>
    </div>
</div>